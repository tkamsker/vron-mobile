Perfect! I've successfully implemented the complete semantic release workflow and CI/CD pipeline for your Flutter project. Here's what I've created:

  Summary

  1. Git Workflow Documentation

  Created docs/git-workflow.md with comprehensive documentation covering:
  - Three-tier branch strategy (dev → stage → main)
  - Conventional commits specification with examples
  - Semantic versioning rules (MAJOR.MINOR.PATCH)
  - User story completion workflow
  - Release process and TestFlight deployment tracks
  - Best practices and troubleshooting

  2. GitHub Actions CI/CD Workflows

  Created 4 workflow files in .github/workflows/:

  a) pr-checks.yml - Pull Request validation:
  - Runs on all PRs to dev, stage, and main
  - Executes tests, code analysis, and formatting checks
  - Validates PR titles follow conventional commit format
  - Verifies iOS build compiles

  b) dev-deploy.yml - Development deployment:
  - Triggers on push to dev branch
  - Builds and deploys to TestFlight (development track)
  - Generates build numbers automatically

  c) stage-deploy.yml - Staging deployment:
  - Triggers on push to stage branch
  - Calculates next version using semantic-release (dry-run)
  - Deploys to TestFlight (staging/beta track)
  - Creates pre-release tags

  d) main-release.yml - Production release:
  - Triggers on push to main branch
  - Runs semantic-release to create GitHub releases
  - Generates CHANGELOG.md automatically
  - Deploys to TestFlight (production track)
  - Creates release notes from conventional commits

  3. Semantic Release Configuration

  Created .releaserc.json with:
  - Conventional commits parsing rules
  - Automatic version bumping (feat→MINOR, fix→PATCH, BREAKING→MAJOR)
  - CHANGELOG generation
  - GitHub release creation
  - Support for main and stage branches

  4. Commitlint Configuration

  Created .commitlintrc.json to enforce conventional commit format with validation rules for commit message structure.

  5. Updated Speckit Tasks

  Modified .claude/commands/speckit.tasks.md to include:
  - Conventional commits requirements
  - User story completion workflow
  - Git workflow and branch strategy
  - Task generation guidelines for commit/PR tasks

  Next Steps

  To activate this CI/CD pipeline, you'll need to:

  1. Set up GitHub repository secrets:
    - APP_STORE_CONNECT_API_KEY - Base64-encoded App Store Connect API Key
    - APP_STORE_CONNECT_ISSUER_ID - App Store Connect Issuer ID
    - APP_STORE_CONNECT_API_KEY_ID - App Store Connect API Key ID
    - MATCH_PASSWORD - Fastlane Match password for code signing
    - MATCH_GIT_URL - Git URL for certificates repository
    - FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD - App-specific password
  2. Set up Fastlane (if not already configured):
    - Create ios/Fastfile with lanes for beta and release
    - Configure Fastlane Match for code signing
  3. Create branches:
  git checkout -b dev
  git push origin dev
  git checkout -b stage
  git push origin stage
  4. Start using conventional commits:
  git commit -m "feat(auth): complete user authentication

  - Email/password login
  - Token management  
  - Offline support

  Closes #US1"

  The workflow is now ready to automate your releases with semantic versioning and TestFlight deployments!