targets:
  $default:
    builders:
      drift_dev:
        enabled: true
        options:
          # Disable manager generation to avoid API compatibility issues
          generate_manager: false
      artemis:
        enabled: false
